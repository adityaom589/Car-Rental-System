# Car-Rental-System
Java console application for managing car rentals — built using OOP principles (encapsulation, separation of concerns) with MySQL + JDBC for persistent storage. Supports viewing available cars, renting, returning, and adding new cars in real time.
